version: '3.4'
services:
  alfresco26-cad-viewer-share:
    image: alfresco-share-alfresco26-cad-viewer:development
    build:
      dockerfile: ./Dockerfile
      context: ../../../alfresco26-cad-viewer-share-docker/target
    environment:
      CATALINA_OPTS: "-agentlib:jdwp=transport=dt_socket,server=y,suspend=n,address=0.0.0.0:8888"
      REPO_HOST: alfresco26-cad-viewer-acs
      REPO_PORT: 8080
    ports:
      - "${share.port}:8080"
      - "${share.debug.port}:8888"
  alfresco26-cad-viewer-acs:
    image: alfresco-content-services-alfresco26-cad-viewer:development
    build:
      dockerfile: ./Dockerfile
      context: ../../../alfresco26-cad-viewer-platform-docker/target
    environment:
      JAVA_TOOL_OPTIONS: "${keystore.settings}"
      JAVA_OPTS: "
        -Dsolr.secureComms=secret
        -Dsolr.sharedSecret=secret
        -Dmessaging.broker.url=\"failover:(nio://alfresco26-cad-viewer-activemq:61616)?timeout=3000&jms.useCompression=true\"
        "
      CATALINA_OPTS: "-agentlib:jdwp=transport=dt_socket,server=y,suspend=n,address=0.0.0.0:8888"
    ports:
      - "${acs.port}:8080"
      - "${acs.debug.port}:8888"
    volumes:
      - alfresco26-cad-viewer-acs-volume:/usr/local/tomcat/alf_data
    depends_on:
      - alfresco26-cad-viewer-postgres
  alfresco26-cad-viewer-postgres:
    image: postgres:16.5
    environment:
      POSTGRES_DB: alfresco
      POSTGRES_USER: alfresco
      POSTGRES_PASSWORD: alfresco
    command: postgres -c max_connections=300 -c log_min_messages=LOG
    ports:
      - "${postgres.port}:5432"
    volumes:
      - alfresco26-cad-viewer-db-volume:/var/lib/postgresql/data
  alfresco26-cad-viewer-ass:
    image: alfresco/alfresco-search-services:2.0.15
    environment:
      SOLR_ALFRESCO_HOST: alfresco26-cad-viewer-acs
      SOLR_ALFRESCO_PORT: 8080
      SOLR_SOLR_HOST: alfresco26-cad-viewer-ass
      SOLR_SOLR_PORT: 8983
      SOLR_CREATE_ALFRESCO_DEFAULTS: alfresco,archive
      ALFRESCO_SECURE_COMMS: "secret"
      JAVA_TOOL_OPTIONS: "-Dalfresco.secureComms.secret=secret"
    ports:
      - "8983:8983"
    volumes:
      - alfresco26-cad-viewer-ass-volume:/opt/alfresco-search-services/contentstore
      - alfresco26-cad-viewer-ass-volume:/opt/alfresco-search-services/data
  alfresco26-cad-viewer-activemq:
      image: alfresco/alfresco-activemq:5.18-jre17-rockylinux8
      mem_limit: 1g
      ports:
        - 8161:8161 # Web Console
        - 5672:5672 # AMQP
        - 61616:61616 # OpenWire
        - 61613:61613 # STOMP

volumes:
  alfresco26-cad-viewer-acs-volume:
    external: true
  alfresco26-cad-viewer-db-volume:
    external: true
  alfresco26-cad-viewer-ass-volume:
    external: true
